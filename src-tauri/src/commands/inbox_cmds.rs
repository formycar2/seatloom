/// Get current Inbox items (handoffs, drift signals, digest items).
#[allow(dead_code)] // scaffold: not yet registered with Tauri invoke handler
pub fn get_inbox() -> Vec<String> {
    // TODO: project via RouteEngine
    vec![]
}

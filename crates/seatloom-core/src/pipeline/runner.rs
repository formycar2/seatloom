// Pipeline runner: executes stages sequentially, emits progress events.
// Full implementation deferred to pipeline-engine packet.
pub struct PipelineRunner;

impl PipelineRunner {
    pub fn new() -> Self { Self }
}

// Git operations: commit, branch, diff, last-commit hash.
// Full implementation deferred to git-ops packet.
#[derive(Default)]
pub struct GitOps;

impl GitOps {
    pub fn new() -> Self {
        Self
    }
}

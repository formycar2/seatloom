// Route engine: Inbox projection and priority band assignment.
// Full implementation deferred to data-engine packet.
#[derive(Default)]
pub struct RouteEngine;

impl RouteEngine {
    pub fn new() -> Self {
        Self
    }
}

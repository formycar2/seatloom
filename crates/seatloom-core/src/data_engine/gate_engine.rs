// Gate engine: transition guards, field validation, and budget gates.
// Full implementation deferred to data-engine packet.
#[derive(Default)]
pub struct GateEngine;

impl GateEngine {
    pub fn new() -> Self {
        Self
    }
}

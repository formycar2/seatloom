// Timeline view projection: ordered CanonicalEvents with template/subtype filter support.
// Full implementation deferred to route-engine packet.
#[derive(Default)]
pub struct TimelineView;

impl TimelineView {
    pub fn new() -> Self {
        Self
    }
}

services:
  postgres:
    image: postgres:16-alpine
    container_name: seatloom-postgres
    environment:
      POSTGRES_USER: seatloom
      POSTGRES_PASSWORD: seatloom
      POSTGRES_DB: seatloom
    ports:
      - "5432:5432"
    volumes:
      - seatloom-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "psql -U seatloom -d seatloom -c 'SELECT 1' -q"]
      interval: 5s
      timeout: 5s
      retries: 20

volumes:
  seatloom-pgdata:
